# DeepSeek — SLM Training System · 21 September 2026

Seated on the Signal Detection door at `public/corps-deepseek.html#slm-training`. Deep links: `#layer-1` through `#layer-5`.

Pretty aliases `/so-mang-tube/slm-training` and `/corps/training` 308 to `/corps-deepseek`. So Mang Tube stays off the public nav.

Public method only. No private EKG, no seed conversations, no training run. `data-location="본진"` marks on-premise intent. The Founder starts training; the page does not.

Battery EKG `#ekg-2026-09-16` and `#tube-battery-triage` stay as they were. No Oreki name.

## Tracks

- `somang-deepseek-lora` — DeepSeek 소망
- `somang-google-lora` — Old Google 소망이 형
